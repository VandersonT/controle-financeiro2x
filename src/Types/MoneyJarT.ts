import { Double } from "react-native/Libraries/Types/CodegenTypes"

export type MoneyJarT = {
    id: string,
    title: string,
    money: Double,
    user_id: string,
    image: string,
    created_at: number
}